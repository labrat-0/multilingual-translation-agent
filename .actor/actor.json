{
  "actorSpecification": 1,
  "name": "multilingual-translation-agent",
  "title": "Multilingual Translation Utility Agent",
  "description": "Translates text between languages using LibreTranslate. Accepts ISO 639-1 language codes, validates inputs, and returns translated text with per-character billing. Supports self-hosted and public LibreTranslate endpoints.",
  "version": "0.2.0",
  "dockerfile": "./Dockerfile",
  "main": "src/agent/main.py",
  "input": "./INPUT_SCHEMA.json",
  "output": {
    "actorOutputSchemaVersion": 1,
    "title": "Translation results",
    "properties": {
      "results": {
        "type": "string",
        "title": "Default dataset results",
        "template": "{{links.apiDefaultDatasetUrl}}/items"
      }
    }
  }
}
